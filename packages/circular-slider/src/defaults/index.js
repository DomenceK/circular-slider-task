/**
 * @param {number} DEFAULT_CIRCLE_OFFSET - Circle offset, default to 10px.
 * @param {number} DEFAULT_STROKE_WIDTH - Width of circle stripe border, defaults to 20px
 * @param {number} DEFAULT_SLIDER_THRESHOLD - Lock threshold for slider lock, defaults to 10 degrees.
 */
export const DEFAULT_CIRCLE_OFFSET = 10
export const DEFAULT_STROKE_WIDTH = 20
export const DEFAULT_SLIDER_THRESHOLD = 10
